---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About — la-bu" description="A little bit about me.">
  <div class="page">
    <header class="page-header">
      <h1>About</h1>
    </header>

    <section class="section">
      <h2>Bio</h2>
      <p>
        Hi, I'm la-bu — a developer interested in building clean, fast, and
        thoughtful software. I care about good design, clear writing, and making
        things that are a pleasure to use.
      </p>
      <p>
        I work mostly in TypeScript and Python, and I'm drawn to the web as a
        platform because of how accessible it is to both builders and users.
      </p>
    </section>

    <section class="section">
      <h2>Currently focused on</h2>
      <ul>
        <li>Building in public — writing about what I learn as I learn it</li>
        <li>Web performance and the craft of minimal interfaces</li>
        <li>Exploring static site generation and content-first web development</li>
      </ul>
    </section>

    <section class="section">
      <h2>A personal note</h2>
      <p>
        Outside of code, I spend time reading, thinking about systems and ideas,
        and occasionally making things with my hands. This site is a place to
        think out loud — equal parts dev blog and digital notebook.
      </p>
      <p>
        If something here resonates, feel free to reach out.
      </p>
    </section>
  </div>
</BaseLayout>

<style>
  .page {
    max-width: var(--max-width);
    margin: 0 auto;
    padding: var(--space-12) var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-12);
  }

  .page-header h1 {
    font-size: var(--text-3xl);
    font-weight: 700;
    line-height: var(--leading-tight);
  }

  .section {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
  }

  h2 {
    font-size: var(--text-xl);
    font-weight: 600;
  }

  p {
    color: var(--color-text);
    line-height: var(--leading-normal);
  }

  ul {
    padding-left: var(--space-6);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  li {
    line-height: var(--leading-normal);
  }

  @media (max-width: 480px) {
    .page {
      padding: var(--space-8) var(--space-4);
      gap: var(--space-8);
    }
  }
</style>
